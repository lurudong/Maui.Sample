namespace HelloWorld
{
    /// <summary>
    /// 此文件定义 AppShell 类，该类用于定义应用的可视层次结构。
    /// </summary>
    public partial class AppShell : Shell
    {
        public AppShell()
        {
            InitializeComponent();
        }
    }
}